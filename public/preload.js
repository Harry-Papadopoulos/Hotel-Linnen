// const { ipcRenderer, contextBridge } = require("electron");

// contextBridge.exposeInMainWorld("electron", {
//   communicationApi: {
//     sendRooms(rooms) {
//       ipcRenderer.send("notify", rooms);
//     },
//   },
// });

// window.ipcRenderer = require("electron").ipcRenderer;
